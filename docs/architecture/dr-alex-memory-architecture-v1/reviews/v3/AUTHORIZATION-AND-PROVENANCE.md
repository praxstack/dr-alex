# V3 authorization and provenance receipt

## Human instruction

The current user instruction, titled “Comprehensive Architecture Review and Implementation
Process,” explicitly directs the agent to review the listed project materials, verify current
architecture, create and review complete specifications, derive tickets, implement only after
approval, close every code-review finding, run all frozen checks, and make final local commits.
The active follow-up goal states that these specifications are pre-approved, requires continuous
completion, and directs any unavailable Grok, OpenCode, or other named-model review to be
replaced by Codex Luna XHigh rather than treated as a blocker.

## Authorized interpretation

That newer direct instruction authorizes creation and implementation of a content-free v3
candidate within its constraints. It does not authorize publication of restricted report bodies,
live clinical-data access, migration, deployment, activation, deletion, or a PWA/TUI retention
policy decision. The original report hashes remain historical provenance only.

Model substitution changes only who executes an already-required role lens. It does not waive a
review, finding, test, threshold, or exact-byte requirement.

The older agent-authored sanitization proposal remains preserved as review history. V3 satisfies
its privacy purpose by giving fresh reviewers only content-free requirements and repository
evidence. No receipt claims that the user approved the proposed PWA or legacy TUI retention
policy; both remain blocked human decisions.

## Hash closure

The ADR status and all governing status strings are finalized before the last conformance review.
After every requested reviewer passes those exact bytes, a separate approval receipt records the
governing v3 and four-file hardening OpenSpec hashes plus the overlap integrity hash before the
first production edit. Any later governing change requires a new version and fresh review.
