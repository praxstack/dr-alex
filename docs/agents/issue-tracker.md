# Issue tracker

GitHub Issues on `praxstack/dr-alex` via `gh`. Specs and tickets publish as issues.

- One issue per unit of work; title states the outcome.
- Apply a triage label on every new issue (see triage-labels.md).
- Close only with evidence in the thread (command output, test name, link).
